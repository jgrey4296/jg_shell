//To be used with mocha --require babel-polyfill --compilers js:babel-register
//Import * as aModule from "../src/aModule";
import * as chai from "chai";
import P from "../src/PParse";

let should = chai.should(),
    expect = chai.expect;

describe ("Shell Parser Tests", function() {

    describe("Raw Parsing", function() {

        before(function(){
            //setup the parser
        });

        //Test each command individually
        
    });

    describe("Parsing Interfaced with shell", function() {

        beforeEach(function(){
            //setup the shell
        });

        afterEach(function(){
            //clear the shell
        });

        //test each command does the right thing
        

    });

    
    
});
